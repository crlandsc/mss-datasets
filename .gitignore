__pycache__/
*.pyc
*.egg-info/
dist/
build/
*.egg
.pytest_cache/
.tox/
.venv/
venv/
.DS_Store
.env
review_list*.md
spec.md
.python-version
data/
CLAUDE.md
.claude/
.cursor/
.vscode/

# Local research notes. This repo is public and these cite private tracker IDs
# and internal repo paths, so they stay out of it.
/research/

# Local configs hold a real Zenodo token, keep only the template tracked
config*.yaml
!config.example.yaml

# Default --data-dir for downloads (tens of GB of dataset archives).
# Anchored, otherwise it would also swallow src/mss_datasets/datasets/.
/datasets/
